syntax = "proto3";

package continuum;

service GameService {
  rpc GetGameStatus(GameRequest) returns (GameResponse);
}

message GameRequest {
  string game_name = 1;
  int64 lamport_timestamp = 2;
}

message GameResponse {
  string status = 1;
  int64 lamport_timestamp = 2;
}